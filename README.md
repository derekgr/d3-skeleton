Skeleton webapp for prototyping locally or on Heroku with D3 and CoffeeScript.

## Usage

- Install bundler.
- `bundle install`
- `bundle exec shotgun`
- Put your data into `public/data`.
- Put your javascript or coffeescript into `assets/javascripts`.
- Edit views under `views`, anything Sinatra will handle out of the box; example provided is erb.
